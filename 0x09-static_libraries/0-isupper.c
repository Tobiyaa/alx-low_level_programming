#include "main.h"
/**
 * _isupper - checkes upper case
 * @c: print int
 * Return: give 1 or 0
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1):
}

else
{
return (0):
}
 /**
 * _isupper - check upper c
 * @c: letter
 * Return: 1 if c is uppercase 0 if not
 */
int _isupper(int c)
{

	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);

}
