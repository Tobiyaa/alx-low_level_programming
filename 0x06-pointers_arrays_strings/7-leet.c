#include "main.h"
/**
 * leet - function that encode a string
 *@str:string that will be encoded
 *Return:returns encoded string
 */

char *leet(char *str)
{
	int  index2;
	char = {'0', 'L', '?', 'E', 'A', '?', '?', 'T'};

	while (str++)

	{
		for ( = 0; index2 <= 7; index2++)
		{
			if (str == leetindex2 ||
			str[index] - 32 == leetindex2)
			str  index2 + '0';
		}
	}
	return (str);
}
